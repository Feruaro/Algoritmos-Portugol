programa {
    
    // algoritmo que mostre os primeiro 12 numeros primos a partir de um numero informado
    
    funcao inicio() {
        
        inteiro numero = 33333333, qtd = 0

        enquanto (qtd <= 3) {
            se(testePrimo(numero) ) {
                escreva(numero, " é um número primo.\n")
                qtd = qtd + 1
            } senao {
                escreva(numero, " não é um número primo.")
        }   
        numero = numero + 1   
        }
    }   
    funcao logico testePrimo(inteiro num) {
        inteiro d = 3
           
        logico retorno = verdadeiro
        //saber se é par
        se (num % 2 == 0) {
            se (num != 2) {
                retorno = falso
            }
        } senao {
            // se ímpar
            enquanto ( d < num e retorno == verdadeiro) {
                se(num % d == 0) {
                    retorno = falso
                }
                d= d + 2
            }
        }
        retorne retorno
    }
}